# PR #501 유월(김예안) (OPEN)

- author: vlmbuyd
- url: https://github.com/woowacourse/react-payments/pull/501
- changedFiles: 41 (+7057 -1)

## 페어 고민 (PR body 핵심 발췌)

- UI와 로직 관심사 분리
- 재사용 가능한 컴포넌트
- 변경에 유연한 컴포넌트
- "에러 메시지는 해당 인풋에 focus 되었을 때만 보여준다. (onBlur 시에는 에러 메시지를 없앤다)"
- cvc 상태를 PaymentForm가 갖고 있게 된다면 불필요한 리렌더링을 유발하므로 CVCFieldForm으로 격리시켜 상태를 관리했습니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 기능 명세 체크리스트 작성
**파일**: `README.md`
```md
// README.md
## 기능 명세

### 카드 프리뷰

- [x] 입력한 카드 유효기간에 따라 카드 프리뷰를 실시간으로 업데이트한다.

### 카드 번호 입력 및 식별

- [x] 카드 번호를 입력 받는다.
- [x] 카드 브랜드를 식별한다.
- [x] [예외] 숫자 외의 값이 입력되는 경우
- [x] [예외] visa, mastercard 외의 번호로 시작하는 경우
```

### 결정 2 — Storybook React Vite 구성
**파일**: `.storybook/main.ts`
```ts
// .storybook/main.ts
import type { StorybookConfig } from '@storybook/react-vite';

const config: StorybookConfig = {
  "stories": [
    "../src/**/*.mdx",
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  "addons": [
    "@chromatic-com/storybook",
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-docs",
    "@storybook/addon-onboarding"
  ],
  "framework": "@storybook/react-vite"
};
```

### 결정 3 — Storybook 접근성 검사 모드 설정
**파일**: `.storybook/preview.ts`
```ts
// .storybook/preview.ts
const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },

    a11y: {
      // 'todo' - show a11y violations in the test UI only
      // 'error' - fail CI on a11y violations
      // 'off' - skip a11y checks entirely
      test: 'todo',
```

### 결정 4 — ESLint·Prettier·Storybook 규칙 연결
**파일**: `eslint.config.js`
```js
// eslint.config.js
import storybook from "eslint-plugin-storybook";

import js from '@eslint/js';
import globals from 'globals';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';
import tseslint from 'typescript-eslint';
import prettier from 'eslint-config-prettier';

export default tseslint.config({ ignores: ['dist', 'storybook-static'] }, {
  extends: [js.configs.recommended, ...tseslint.configs.recommended, prettier],
  files: ['**/*.{ts,tsx}'],
  languageOptions: {
    ecmaVersion: 2020,
    globals: globals.browser,
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `src/components/CardPreview/CardPreview.tsx:1`: "잘못된 import: `storybook/theming` 대신 `@emotion/styled` 사용 필요" → 답변 없음
- @coderabbitai[bot] `src/components/Common/ErrorMessage/ErrorMessage.tsx:6`: "오류 메시지의 스크린리더 알림 속성 추가 필요" → 답변 없음
- @coderabbitai[bot] `src/components/Common/Form/FormField.tsx:67`: "입력 필드 `id`가 중복될 수 있습니다" → 답변 없음
- @coderabbitai[bot] `src/components/Form/PaymentForm.tsx:84`: "`<form>` 요소에 `onSubmit` 핸들러가 없습니다" → 답변 없음
- @coderabbitai[bot] `src/components/Layout/ServiceLayout.tsx:18`: "CSS padding 선언 문법 오류를 수정하세요." → 답변 없음
- @coderabbitai[bot] `src/constants/index.ts`: "에셋 경로 형식이 일관되지 않습니다" → 답변 없음

## 잠재 안티패턴 태그

[state-shape]: CVC 상태 하위 격리 논의
[validation]: 길이 미달을 MAX_LENGTH로 표시
[naming]: validate*가 실패 시 true 반환
[input-handling]: CVC 길이 3 규칙 중복
[styling]: 100vw·padding 문법 지적
[card-brand]: 카드사 판별 로직 중복 신호