# PR #497 안톨리니(이시현) (OPEN)

- author: Antoliny0919
- url: https://github.com/woowacourse/react-payments/pull/497
- changedFiles: 42 (+7678 -9)

## 페어 고민 (PR body 핵심 발췌)

- 상태를 어디에 두는게 적절할까? 에대한 고민을 많이 했습니다.
- Props Drilling과 관련해서 고민했습니다.
- 검증로직과 같은 도메인 로직을 어디에 두는게 좋을지 고민했습니다.
- 리팩토링을 하는 과정에서 이벤트로직에 대한 반복되는 플로우를 추상화 하려고 했습니다.
- 처음부터 설계를 어느정도 해두고 **"돌아가는 쓰레기를 만들자"** 라는 목표를 설정하여 구현하는데에는 정말 거의 막힘이 없었던거 같습니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 기능 요구 사항 체크리스트 정리
**파일**: `README.md`
```md
<!-- README.md -->
# 페이먼츠 미션

## 기능 요구 사항

- 컴포넌트
  - [x] CardForm
    - [x] CardSection
      - [x] CardNumberInputComponent
      - [x] CardExpiryDateInputComponent
      - [x] CardCVCInputComponent
```

### 결정 2 — Storybook React Vite 구성
**파일**: `app/.storybook/main.ts`
```ts
// app/.storybook/main.ts
import type { StorybookConfig } from '@storybook/react-vite';

const config: StorybookConfig = {
  "stories": [
    "../src/**/*.mdx",
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  "addons": [
    "@chromatic-com/storybook",
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-docs"
  ],
  "framework": "@storybook/react-vite"
};
export default config;
```

### 결정 3 — 접근성 테스트 파라미터 설정
**파일**: `app/.storybook/preview.ts`
```ts
// app/.storybook/preview.ts
import type { Preview } from '@storybook/react-vite'

const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
       color: /(background|color)$/i,
       date: /Date$/i,
      },
    },

    a11y: {
      test: 'todo'
    }
  },
};
```

### 결정 4 — React 앱 엔트리 HTML 추가
**파일**: `app/index.html`
```html
<!-- app/index.html -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>app</title>
  </head>
  <body>
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `app/index.html:2`: "문서 언어 설정이 실제 UI 언어와 불일치합니다." → `lang="en"`을 `ko`로 변경 제안
- @cys4585 `app/src/components/form/CardNumberInput.tsx:37`: "networkBrand 는 cardNumber['first-digits'] 에서 바로 유도 가능한 값" → 별도 상태 대신 유도값 검토 요청
- @cys4585 `app/src/components/form/CardNumberInput.tsx:25`: "에러 상태를 필드별 state 4개 + 공용 message 1개 로 모델링" → 메시지 공백 현상 질문
- @coderabbitai[bot] `app/src/validators/CardValidator.ts:5`: "Number(value) 기반 체크는 '1e2', '+', 공백 같은 값을 통과" → 숫자 문자 검증 좁히기 제안
- @cys4585 `app/src/components/Card.tsx:30`: "Context 도입의 대가 — 트레이드오프도 있을 텐데" → Provider 주입·의존성 노출 등 질문

## 잠재 안티패턴 태그

[state-shape]: 에러 state와 message 분리 신호
[state-update]: networkBrand 유도 가능 상태
[validation]: Number(value) 숫자 검증 사용
[input-handling]: 자동 포커스 요구사항 누락
[card-brand]: prefix 기반 카드사 식별 상태
[testing]: play 스토리 assertion 누락