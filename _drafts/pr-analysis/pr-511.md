# PR #511 클라우디(박종운) (OPEN)

- author: bel1c10ud
- url: https://github.com/woowacourse/react-payments/pull/511
- changedFiles: 42 (+7145 -10)

## 페어 고민 (PR body 핵심 발췌)

- "이번 미션에서는 페어가 React에 익숙하지 않아 제가 상대적으로 더 주도적으로 설계하고 설득하는 형태가 되었던 것 같습니다."
- "현재 각 입력 컴포넌트의 에러 메시지 출력 위치가 시안과 조금 다릅니다."
- "다만 페어가 리액트를 막 시작한 시점이었던 만큼, 구현 난이도가 높은 컴포넌트를 만드는 것을 만드는것을 목표로하기에는 학습 부담이 크다고 판단했습니다."
- "`연관된 상태는 하나로 관리하라`라는 원칙을 알게되어 카드 번호, 만료월, 만료년도, CVC를 하나의 객체 상태로 다루도록 변경하도록 하였습니다."
- "ValidationInput 내부에서 \"limit\"은 onChange 시점에 \"check\"는 onBlur 시점에 처리하도록 구분하였습니다."

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 카드 번호 4분할 입력 요구사항 정리
**파일**: `README.md`
```md
// README.md
# 페이먼츠 미션

## 기능 요구 사항

### 카드 번호 입력 및 식별

- [x] 카드 번호는 4자리씩 4개의 input으로 입력받는다
- [x] 입력은 숫자만 가능하고 유효하지 않은 번호 입력시 피드백을 제공한다.
```

### 결정 2 — 유효기간과 CVC 입력 규칙 명시
**파일**: `README.md`
```md
// README.md
### 카드 유효기간 입력

- [x] 카드 유효기간은 월 2자리와 년도 2자리를 각각 별도의 input으로 입력받는다.
- [x] 입력은 유효한 월과 년도만 입력가능하도록 제한한다.

### CVC

- [x] CVC는 숫자 3자리만 입력 가능하다.
- [x] 유효하지 않은 번호 입력시 제한한다.
```

### 결정 3 — Storybook 기반 컴포넌트 상태 확인
**파일**: `.storybook/main.ts`
```ts
// .storybook/main.ts
import type { StorybookConfig } from '@storybook/react-vite';

const config: StorybookConfig = {
  "stories": [
    "../src/**/*.mdx",
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  "addons": [
    "@chromatic-com/storybook",
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-docs",
    "@storybook/addon-onboarding"
  ],
  "framework": "@storybook/react-vite"
};
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `src/components/CardExpiryDateInput.tsx:33`: "부모 상태와 분리된 내부 상태로 인해 값 동기화가 깨질 수 있습니다." → 다음 단계 때 참고 가능
- @coderabbitai[bot] `src/components/CardForm.tsx:23`: "함수형 업데이트를 사용하지 않아 상태 업데이트 유실 가능성이 있습니다." → 정보 없음
- @coderabbitai[bot] `src/components/Common/ValidationInput.tsx:25`: "커스텀 props를 DOM 요소에 직접 전파하지 않도록 분리하세요." → 정보 없음
- @coolchaem `src/components/CardCVCInput.tsx:34`: "반복되는 `validations` 조합을 어떻게 관리할지입니다." → ValidationInput 책임 범위 재검토
- @coolchaem `src/components/Common/ValidationInput.tsx:74`: "에러 메시지 렌더링 위치는 상위 도메인 컴포넌트에서 결정" → 에러를 배열이나 객체로 관리해 개선 예정
- @coolchaem `src/utils.ts:3`: "숫자 문자열 검증에서 공백이 통과하는 이슈를 발견하였어요" → CodeRabbit 코멘트 확인 요청

## 잠재 안티패턴 태그

- [state-shape] : 도메인 input 내부 상태 동기화
- [state-update] : props.formState spread 업데이트
- [validation] : 공백 숫자 검증 통과
- [component-split] : ValidationInput 에러 렌더 책임
- [type-safety] : React 네임스페이스 미import
- [card-brand] : 정규식 g 플래그/브랜드 오인식