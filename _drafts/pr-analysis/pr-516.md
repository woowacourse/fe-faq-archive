# PR #516 레스(이어진) (OPEN)

- author: lee-eojin
- url: https://github.com/woowacourse/react-payments/pull/516
- changedFiles: 35 (+7586 -0)

## 페어 고민 (PR body 핵심 발췌)

- 이번 단계에서 가장 많이 고민했던 것은 컴포넌트 구조 설계였습니다.
- 그룹화와 검증은 서로 다른 책임이라고 판단해서 둘 다 두기로 했습니다.
- 세 섹션이 구조는 같지만 로직은 조금씩 다르다는 점이었습니다.
- 입력값 상태(`inputValues`)가 두 곳에 존재하게 되었습니다.
- `brand`는 `cardInfo.numbers[0]`으로부터 파생되는 값인데, 에러 상태와 마찬가지로 굳이 `useEffect`를 통해 상태로 저장해야 하는지도 고민이 됩니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 정보 없음
**파일**: `정보 없음`
```text
정보 없음
```

## 리뷰어 피드백 (실제 인용)

- @eastroots92 `my-app/src/components/ValidatedInputGroup/ValidatedInputGroup.tsx:null`: "너무 내부에서 많은 일들을 하려고 하는 것 같아요." → 확장성을 고려하는 형태 요청
- @eastroots92 `my-app/src/components/CardNumberInputSection/CardNumberInputSection.tsx:null`: "let을 사용하지 않고 지금보다 더 가독성 좋게 바꿔 볼 수 있을까요?" → `getValidationError` 분리했다고 답변
- @eastroots92 `my-app/src/components/ExpiryDateInputSection/ExpiryDateInputSection.tsx:null`: "validate를 별도의 함수, 변수로 만들어서" → 정보 없음
- @coderabbitai[bot] `my-app/src/App.tsx:null`: "디버그용 `console.log` 제거 필요" → `4689c763`
- @coderabbitai[bot] `my-app/src/components/ValidatedInputGroup/ValidatedInputGroup.tsx:36`: "`inputMode=\"numeric\"` 누락" → 정보 없음
- @coderabbitai[bot] `my-app/src/components/InputSectionLayout/InputSectionLayout.tsx:null`: "`<label>` 접근성 연결 누락" → 정보 없음

## 잠재 안티패턴 태그

- [component-split] : ValidatedInputGroup 책임 과다 신호
- [state-shape] : inputValues와 cardInfo 이중 보유
- [effect] : brand 파생값을 useEffect로 저장
- [validation] : CVC 자릿수 검증 누락
- [input-handling] : inputMode numeric 누락
- [type-safety] : string[]/brand string 제약 약함