# PR #520 지오(장주형) (OPEN)

- author: JuHyeong424
- url: https://github.com/woowacourse/react-payments/pull/520
- changedFiles: 46 (+7372 -13)

## 페어 고민 (PR body 핵심 발췌)

- 초기 구현에서는 CardInfo 컴포넌트 하나에 모든 입력 상태(useState), 이벤트 핸들러(onChange), 그리고 복잡한 유효성 검사 로직(숫자 여부 확인, 월/년도 범위 검사 등)을 전부 몰아넣었습니다.
- 이 문제를 해결하기 위해 관심사의 분리를 핵심 목표로 두고 리팩토링을 진행했습니다.
- 순수 함수로의 분리 (Validator): 리액트 상태와 무관하게 입력값의 에러 여부만 판별하는 로직은 순수 자바스크립트 함수로 분리하여 validator.ts에 위임했습니다.
- 커스텀 훅(Custom Hook) 도입: 폼의 입력값 상태와 에러 메시지 업데이트 로직은 useInputHandle, useExpireDate 등의 커스텀 훅으로 추출하여 묶어냈습니다.
- 현재 결제 폼에서 사용자가 input에 값을 입력할 때마다 실시간(`onChange`)으로 유효성 검사를 수행하여 에러 문구를 화면에 바로 노출하고 있습니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 카드 브랜드 식별 요구사항 문서화
**파일**: `README.md`
```md
<!-- README.md -->
### 카드 번호 입력 및 브랜드 식별

| 브랜드     | 식별 규칙                        |
| ---------- | -------------------------------- |
| Visa       | `4`로 시작하는 16자리 숫자       |
| MasterCard | `51`~`55`로 시작하는 16자리 숫자 |

- 숫자(0~9)만 입력 가능
- 4자리씩 4개의 input으로 구성
- 하나의 input에 4자리가 모두 입력되면 카드 프리뷰를 업데이트한다
- 유효하지 않은 번호 입력 시 에러 피드백 제공
```

### 결정 2 — 입력 제한 전략 기록
**파일**: `README.md`
```md
<!-- README.md -->
### 입력 제한 전략

`<input type="number">`가 아닌, `type="text"`에 `inputMode="numeric"`을 조합하고 onChange 핸들러에서 숫자 외 입력을 필터링하는 방식을 고려한다.

> `type="number"`는 `e`, `+`, `-`, `.` 등의 입력을 허용하고, 앞자리 `0` 보존이 안 되는 등의 문제가 있어 카드 입력에는 적합하지 않을 수 있다.
```

### 결정 3 — Storybook 기반 시각 테스트 설정
**파일**: `.storybook/main.ts`
```ts
// .storybook/main.ts
import type { StorybookConfig } from '@storybook/react-vite';

const config: StorybookConfig = {
  "stories": [
    "../src/**/*.mdx",
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  "addons": [
    "@chromatic-com/storybook",
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-docs",
    "@storybook/addon-onboarding"
  ],
  "framework": "@storybook/react-vite"
};
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `src/components/cardInfo/useExpireDate.ts:34`: "월 검증 실패 시에도 state가 갱신됩니다." → 실패 시 early return 권장
- @coderabbitai[bot] `src/components/cardInfo/useInputHandle.ts:29`: "`prev` 상태를 기준으로 업데이트하세요." → 함수형 업데이트에서 prev 사용 권장
- @coderabbitai[bot] `src/components/cardPreview/CardPreview.tsx:29`: "첫 카드 그룹이 비어 있을 때를 방어하세요." → 기본값 또는 non-empty 타입 권장
- @coderabbitai[bot] `src/components/cardPreview/utils.ts:5`: "번들 빌드 환경을 고려하여 에셋 참조 방식을 수정하세요." → import/new URL 방식 권장
- @coderabbitai[bot] `src/components/cardPreview/utils.ts:5`: "Mastercard 판별 범위가 너무 좁습니다." → 2221~2720 범위 검토 권장
- @coderabbitai[bot] `src/components/cardPreview/cardNumber/utils.ts:1`: "입력 배열을 직접 바꾸지 않는 쪽이 더 안전합니다." → 새 배열 반환 권장

## 잠재 안티패턴 태그

- [state-update] : 외부 상태 복사·invalid 값 저장
- [validation] : 월 검증 실패 후 상태 반영
- [type-safety] : 빈 배열 cardNumber 방어 누락
- [card-brand] : Mastercard BIN 범위 협소
- [styling] : 유효하지 않은 CSS 값 사용
- [testing] : 테스트 생성 제안만 존재