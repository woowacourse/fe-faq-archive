# PR #514 벤지(김현중) (MERGED)

- author: hjkim0905
- url: https://github.com/woowacourse/react-payments/pull/514
- changedFiles: 46 (+8179 -9)

## 페어 고민 (PR body 핵심 발췌)

- 가장 많이 고민했던 문제는 props drilling이 적절한가였습니다.
- 검증 로직을 어디에 둘 것인가에 대한 고민이 있었습니다.
- 커스텀 훅을 사용해 props로 넘겨주는 값들을 묶어서 전달하면 코드가 더 깔끔해질 것이라 생각했습니다.
- 각 입력 필드의 유효성 상태를 개별 컴포넌트가 소유할지, 상위에서 통합 관리할지에 대한 고민은 이번 미션의 핵심 트레이드오프 중 하나라고 생각했습니다.
- 상태를 어떤 자료구조로 표현할 것인지의 기준을 어떻게 잡아야 하는지 혼란스러워서 여쭤보고 싶습니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 요구사항을 체크리스트로 관리
**파일**: `README.md`
```md
<!-- README.md -->
# 페이먼츠 미션

## 기능 요구 사항

- 컴포넌트
  - [x] CardForm
    - [x] CardSection
      - [x] CardNumberInputComponent
      - [x] CardExpiryDateInputComponent
      - [x] CardCVCInputComponent
```

### 결정 2 — Storybook 스토리 탐색 범위와 애드온 설정
**파일**: `app/.storybook/main.ts`
```ts
// app/.storybook/main.ts
import type { StorybookConfig } from '@storybook/react-vite';

const config: StorybookConfig = {
  "stories": [
    "../src/**/*.mdx",
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  "addons": [
    "@chromatic-com/storybook",
    "@storybook/addon-vitest",
    "@storybook/addon-a11y",
    "@storybook/addon-docs"
  ],
  "framework": "@storybook/react-vite"
};
```

### 결정 3 — Storybook a11y 검사 모드 설정
**파일**: `app/.storybook/preview.ts`
```ts
// app/.storybook/preview.ts
const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
       color: /(background|color)$/i,
       date: /Date$/i,
      },
    },

    a11y: {
      test: 'todo'
    }
  },
};
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `app/src/components/form/CardCVCInput.tsx:49`: "에러 테두리가 실제 입력에 연결되지 않았습니다." → `CardInput`에 `isError` 전달 제안
- @jw-r `app/src/components/form/CardNumberInput.tsx:18`: "이 로직이 CardValidator.ts의 isValidNetworkBrand와 중복됩니다." → `detectNetworkBrand` 같은 순수 함수 분리 제안
- @jw-r `app/src/validators/CardValidator.ts:1`: "검증 실패를 throw로 처리하는 방식에 대해 고민해볼 부분이 있어요." → `{ valid, message }` 반환값 패턴 제안
- @jw-r `app/src/components/preview/CardNetworkBrand.tsx:5`: "리터럴 유니온 타입을 쓰면 잘못된 값이 들어오는 것을 컴파일 타임에 잡을 수 있어요!" → `type NetworkBrand = 'visa' | 'master'` 제안
- @jw-r `app/src/validators/CardValidator.ts:45`: "같은 연도일 경우 \"월\"까지 비교해야 정확한 만료 판정이 됩니다." → 월+연도 조합 검증 제안

## 잠재 안티패턴 태그

[state-shape] : 오류 메시지 상태가 필드별로 분리되지 않음
[validation] : 검증 실패를 throw/catch로 처리
[type-safety] : networkBrand가 string으로 열려 있음
[component-split] : CardInput 반복 렌더링 중복
[card-brand] : 브랜드 판별 로직 중복
[input-handling] : Number 기반 숫자 검증 사용