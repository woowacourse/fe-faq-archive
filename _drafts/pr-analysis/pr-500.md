# PR #500 먼지(윤선정) (OPEN)

- author: stardusty-lab
- url: https://github.com/woowacourse/react-payments/pull/500
- changedFiles: 79 (+10116 -1)

## 페어 고민 (PR body 핵심 발췌)

- 코드 컨벤션(폴더, 파일명 등)을 미리 정하고 프로젝트를 시작했습니다.
- 프로젝트의 폴더 구조를 시작하기 전에 페어와 고민해서 만들었습니다.
- 상태를 어디에서(부모인 payments와 개별 컴포넌트를 만들어서 외부에서 필요 없는 경우(CVC) 내부에서 state로 관리하면 좋을지 고민했습니다.
- component 를 객체의 property로 같이 넘기는 형태로 만들어서 사용해도 될까요?
- 사용자가 입력하는 값을 담는 state 만 가지고 error 유무를 계산하는 데이터는 state로 가지고 있지 않고 그냥 사용자 입력값을 가지고 계산해서 사용하게 좋을지 고민이 됩니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — CVC 입력 차단 함수
**파일**: `src/pages/payments/Payments.tsx`
```tsx
// src/pages/payments/Payments.tsx
const preventCvc = (cvc: string) => {
  if (!isNumericString(cvc)) return false;
  if (cvc.length > 3) return false;
  return true;
};

const handleChangeCvc = (value: string) => {
  if (!preventCvc(value)) return;
  setCvc(value);
};
```

### 결정 2 — Input blur 이벤트 타입
**파일**: `src/core/components/input/Input.tsx`
```tsx
// src/core/components/input/Input.tsx
onBlur?: (e: React.ChangeEvent<HTMLInputElement>) => void
...
onBlur={onBlur}
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `src/pages/payments/Payments.tsx:123`: "CVC 입력을 빈 문자열로 되돌릴 수 없는 버그가 있습니다." → 답변 없음
- @cys4585 `src/pages/payments/Payments.tsx:113`: "백스페이스로 지우려 하면 마지막 한 글자가 남는 버그" → 답변 없음
- @cys4585 `src/pages/payments/Payments.tsx:111`: "preventCvc 만 true/false 의미가 반대인 것 같아요" → 답변 없음
- @coderabbitai[bot] `src/pages/payments/validator.ts:23`: "유효기간 검증이 ‘형식’까지만 확인하고 있어" → 답변 없음
- @cys4585 `src/pages/payments/Payments.tsx:24`: "유효하지 않은 번호 입력 시 피드백 제공" → 답변 없음
- @coderabbitai[bot] `src/core/components/input/Input.tsx:11`: "`onBlur` 이벤트 타입을 `React.FocusEvent`로 수정" → 답변 없음
- @coderabbitai[bot] `src/core/components/formGroup/FormGroup.tsx:13`: "`fieldset`에 `legend`가 없어 그룹 레이블 전달이 약합니다." → 답변 없음
- @cys4585 `src/core/components/formContainer/FormContainer.tsx:13`: "두 컴포넌트로 나눠서 체감되는 이점이 있으신가요?" → 답변 없음

## 잠재 안티패턴 태그

- [validation] : CVC 삭제 불가·만료일 형식만 검증
- [input-handling] : 숫자 필드 type 기준 불명확
- [type-safety] : onBlur가 ChangeEvent로 선언됨
- [component-split] : FormContainer 분리 근거 질문
- [effect] : error state 동기화 effect 고민
- [docs] : README 플레이스홀더 경로 잔존