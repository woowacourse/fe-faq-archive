# PR #513 아지(송아영) (MERGED)

- author: gustn99
- url: https://github.com/woowacourse/react-payments/pull/513
- changedFiles: 38 (+7827 -1)

## 페어 고민 (PR body 핵심 발췌)

- 이번 페어 프로그래밍은 설계 측면에서의 논의를 많이 나누었어요.
- 이번 미션에서는 페이지 레이어는 컴포넌트들을 조합하는 역할만 수행하고, 구체적인 인풋 검증 로직은 개별 필드 내부에 두는 것으로 페어와 결정을 내렸습니다.
- 저희는 '숨겨진 formValue'로써 cardBrand를 해석하기로 결정했기 때문에 응집도 측면에서 cardBrand를 formValue state 내부에 두었어요.
- 따라서 title + caption + children + error 구조를 가지는 공통 FormField 컴포넌트를 작성해 합성하는 방식으로 구현했고, 좋은 선택이었다고 생각합니다.
- 현재 로직은 currnetIndex를 기반으로 가장 최근 input에 대한 에러를 표시합니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 페이지가 폼 값과 카드 렌더링을 관리
**파일**: `docs/architecture.md`
```md
// docs/architecture.md
# Page

> formValue와 (step2에서 추가될 step..)을 관리함.

## props

- x

## state

- formValue: {
  cardNumbers: {value: ["","","",""]},
  ExpirationPeriod: {value: ["",""]},
  cvcNumbers: {value: ""},
  cardBrand: {value: "" (visa | master | local)},
```

### 결정 2 — 도메인 필드가 입력 검증과 formValue 업데이트 담당
**파일**: `docs/architecture.md`
```md
// docs/architecture.md
# CardNumbersField

> 카드 번호 도메인 로직을 포함한 input 입력, 유효성 검사, formValue 업데이트를 진행함.

## props

- { handleFormValueUpdate: (field, value) => void; }

## state

- error, errorMessage

## functions

- handleFieldChange: 숫자 입력 외 방어, handleFomValueUpdate 호출
```

### 결정 3 — 공통 Field는 children 합성 구조
**파일**: `docs/architecture.md`
```md
// docs/architecture.md
# Field(공통)

> 라벨, 에러메시지 레이아웃

## props

- { title: string; caption: string; error: boolean; errorMessage: string; children: React.Node??; }

## state

- x
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `src/components/domain/CardNumbersField.tsx:16`: "다른 카드 번호 칸의 오류를 현재 칸 입력이 덮어쓰고 있습니다." → 세그먼트별 오류 관리 또는 전체 재계산 제안
- @coderabbitai[bot] `src/pages/AddCardPage.tsx:52`: "`<form>` 요소에 제출 기본 동작을 방지하는 처리를 추가하세요." → onSubmit preventDefault 및 함수형 업데이트 제안
- @jw-r `src/types.ts:6`: "튜플 타입을 사용하면 컴파일 타임에 길이를 보장할 수 있어요!" → 고정 길이 배열 타입 제안
- @jw-r `src/pages/AddCardPage.tsx:28`: "이전 상태를 기반으로 업데이트할 때는 함수형 업데이트를 사용하는 것이 안전합니다." → setFormValue(prev => ...) 제안
- @jw-r `src/pages/AddCardPage.tsx:36`: "cardBrand를 formValue에서 빼고, 렌더링 시점에 매번 계산하는 파생값으로 관리" → 파생값 관리 제안
- @jw-r `src/components/ui/Card.tsx:22`: "map으로 렌더링하는 각 <span>에 key prop이 빠져 있네요!" → key prop 추가 필요

## 잠재 안티패턴 태그

- [validation] : 단일 errorStatus가 오류 덮어씀
- [state-update] : setFormValue prev 미사용
- [state-shape] : cardBrand 파생값을 formValue에 저장
- [type-safety] : 고정 길이 배열이 string[]
- [input-handling] : form submit 기본 동작 미차단
- [testing] : 테스트 스크립트 부재