# PR #499 비비빙(이병관) (OPEN)

- author: binggwa
- url: https://github.com/woowacourse/react-payments/pull/499
- changedFiles: 47 (+8438 -0)

## 페어 고민 (PR body 핵심 발췌)

- 페어와 함께 오랫동안 고민했던 문제는 주로 상태 관리에 관한 내용이었습니다.
- 결국 각 섹션 컴포넌트 내부에서 onBlur를 통해 에러 상태를 관라하기 위해 섹션별로 나누어서 작성했는데요
- 저희는 수업을 통한 공식문서 읽기를 통해 상태는 같이 업데이트되는 것들끼리 묶어야 한다고 생각했기에 따로 관리하는 쪽으로 진행했는데, 동키콩이 보기엔 어느 방향이 괜찮았을지 궁금합니다.
- 그렇다면 과연 app이 Cvc 상태를 알고있어야 하는가 라는 의문이 들었습니다.
- NumberInput 부분은 개수가 모두 다르기에 children으로 주입받도록 설게했습니다.

## 핵심 코드 결정/패턴 (실제 diff 발췌)

### 결정 1 — 기능 목록 선정
**파일**: `README.md`
```md
// README.md
# 구현할 기능 목록
## 카드 UI
- 브랜드 검증
  - 카드번호 1번째 폼에 따라 visa or master 표시
- 카드번호 3번째 폼부터는 **** 로 표시
- 월 입력 시 MM/ 표시
- 연 입력 시 MM/YY 표시
```

### 결정 2 — 카드 입력 섹션 분리 계획
**파일**: `README.md`
```md
// README.md
## 카드 번호 입력 컴포넌트
- 카드번호 검증

## 유효기간 입력 컴포넌트
- 월 검증
- 연 검증

## CVC 입력 컴포넌트
- CVC 검증
```

### 결정 3 — 공통 섹션 컴포넌트 의도
**파일**: `README.md`
```md
// README.md
## Input 폼
- Number 검증
- 에러 체크 기능
  - 에러 발생 시 테두리 빨간색으로 변경
  - 에러 발생 시 하단에 에러 메시지 표시 필요

## 구조 관련 하고 싶은 것
- 3개의 반복되는 Section 별로 공통 컴포넌트를 만들어 사용하고 싶다.
```

## 리뷰어 피드백 (실제 인용)

- @coderabbitai[bot] `my-app/src/utils/Validation.ts:13`: "`isValidRange`가 초과 길이를 유효로 처리합니다." → 답변 정보 없음
- @JUDONGHYEOK `my-app/src/utils/Validation.ts:11`: "이함수의 이름과 로직이 매칭이 안되는 것 같은데요." → 답변 정보 없음
- @JUDONGHYEOK `my-app/src/components/expirationDateSection/ExpirationDateSection.tsx:25`: "검증, 계산, 상태변경등으로 로직을 분리가능해보입니다!" → 답변 정보 없음
- @JUDONGHYEOK `my-app/src/components/common/numberInput/NumberInput.tsx:14`: "maxLength를 DOM 속성으로도 두는 건 어떨까요?" → 답변 정보 없음
- @JUDONGHYEOK `my-app/src/components/common/commonSection/CommonSection.tsx:7`: "요기 any타입을 사용해주신 이유가 있으실까요??" → 답변 정보 없음
- @JUDONGHYEOK `my-app/src/components/cardPreview/CardPreview.tsx:24`: "함수 직접 호출보다 <EachCardNumber ... /> 컴포넌트 형태" → 답변 정보 없음

## 잠재 안티패턴 태그

- [state-shape] : App에서 상태 분리/통합 고민
- [validation] : 에러 상태와 메시지 동기화 고민
- [type-safety] : CommonSection any 사용
- [component-split] : 검증/계산/렌더링 혼재
- [input-handling] : maxLength DOM 속성 누락
- [card-brand] : Mastercard BIN 범위 누락