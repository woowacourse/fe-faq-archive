---
title: 'B-2. 테스트 분리 — 테스트 파일을 나눠야 할까?'
---

import { Reveal } from '../../components/Reveal'

# B-2. 도메인 테스트와 UI 테스트 분리 — 테스트 파일을 나눠야 할까?

## 선배 크루의 고민

> "TDD만으로는 알 수 없는 오류가 있다는 걸 느꼈습니다. 단위 테스트는 통과하는데 실제 UI에서는 문제가 생기는 경우가 있어서, 어디까지 테스트해야 하는지 고민됩니다." — 기린, [PR #356](https://github.com/woowacourse/javascript-lotto/pull/356)

> "unit test만으로는 부족하다고 느꼈습니다. 컨트롤러를 통해 도메인 객체들이 연결되면서 생기는 문제를 잡으려면 통합 테스트가 필요한 것 같은데, 어떻게 구성해야 할지 모르겠습니다." — 쿠마, [PR #200](https://github.com/woowacourse/javascript-lotto/pull/200)

> "함수가 하나의 기능만 하는지 판단하는 기준이 애매합니다. 테스트를 작성하다 보면 하나의 함수에 여러 검증이 필요한 경우가 많은데, 이게 함수 분리의 신호인지 궁금합니다." — 기린, [PR #356](https://github.com/woowacourse/javascript-lotto/pull/356)

## 리뷰어의 피드백

> "기능 테스트와 UI 테스트의 파일을 분리해보는 건 어떨까요~? 파일 하나에 있으니 찾기 어려워 지는 것 같아요!" — PR #44 ([링크](https://github.com/woowacourse/javascript-lotto/pull/44))

> "현재 step2-index.js파일에서 너무 많은 정보를 알고 있어야하고 이는 곧 변경에 취약한 설계라는 것을 의미합니다. 적절하게 도메인과 UI 객체를 분리해서 객체에게 메세지를 보내는 형식으로 변경해 보는 것은 어떨까요?" — PR #395 ([링크](https://github.com/woowacourse/javascript-lotto/pull/395))

> "Controller의 역할에 대해 다시한번 생각할 필요가 있을 것 같아요. 현재는 입력검증, 로직처리, 흐름제어등을 모두 직접적으로 하고 있기 때문에 Controller가 간단한 로직임에도 불구하고 많이 무거워진 상태입니다. Controller가 세부로직들을 모두 알 필요가 있을까요?" — PR #356 ([링크](https://github.com/woowacourse/javascript-lotto/pull/356))

## AS-IS 코드

이렇게 한 곳에 모인 코드는, 테스트도 한 파일에 몰리게 됩니다.

> 출처: [PR #234 — 부엉이 (2023, 2단계)](https://github.com/woowacourse/javascript-lotto/pull/234) · [해당 파일](https://github.com/woowacourse/javascript-lotto/blob/b6231ed7fca3fc82b3c50e2f9ce12ae4aad1b88d/src/js/controller/stepTwo/WebLottoController.js)

```js
// controller/stepTwo/WebLottoController.js — 컨트롤러가 DOM 접근 + 검증 + 도메인 로직 + UI 렌더링을 모두 수행
buyLotto() {
  try {
    ui.hideMoneyValidationText();
    const moneyInput = domList.moneyInput.value; // 컨트롤러가 DOM에 직접 접근
    inputValidator.validateMoneyInput(moneyInput);
    this.#lottoMachine.buyLotto(+moneyInput);
    ui.showRestUI(this.#lottoMachine.lottos);     // UI 렌더링도 여기서
  } catch (error) {
    // ...
  }
}

bindAllEvents() {
  // 컨트롤러가 DOM 요소를 직접 알고 이벤트를 바인딩
  this.addEvent({ element: ui.getDomWithName('buyBtn'), event: () => this.buyLotto(), type: 'click' });
  this.addEvent({ element: ui.getDomWithName('resultBtn'), event: () => this.calculateStatistics(), type: 'click' });
  this.addEvent({ element: ui.getDomWithName('retryBtn'), event: () => this.restartGame(), type: 'click' });
}
```

### 이 코드를 보면서 이야기해봅시다

- "이 `buyLotto()`를 테스트하려면 무엇을 준비해야 하나요? (DOM, validator, lottoMachine, ui) 이 중 도메인 로직만 테스트하고 싶다면?"
- "이 함수를 역할별로 쪼갠다면, 각각의 테스트 파일은 어떻게 나뉠까요?"

---

<Reveal>

## 탐색 미션

### PR #44 — 피터 (2021, 2단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/44)

**찾아볼 것:**
1. "리뷰어(발리스타)가 '기능 테스트와 UI 테스트의 파일을 분리해보는 건 어떨까요'라고 한 이유는 무엇인가요?"
2. "한 파일에 있으면 '찾기 어려워진다'는 것 외에, 분리하면 어떤 이점이 더 있을까요?"

### PR #126 — 돔하디 (2022, 2단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/126)

**찾아볼 것:**
1. "컨트롤러가 비대해지면 테스트 코드는 어떤 문제에 직면하게 되나요? 리뷰어가 이 문제를 어떻게 설명했나요?"
2. "Model, View, Controller 각 계층의 테스트를 독립적으로 작성하려면 코드를 어떻게 분리해야 할까요?"

### PR #395 — 기린 (2025, 2단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/395)

**찾아볼 것:**
1. "리뷰어가 '너무 많은 정보를 알고 있어야 한다'고 지적한 파일은 무엇이고, 어떤 종류의 정보가 섞여 있나요?"
2. "'객체에게 메세지를 보내는 형식'이란 구체적으로 어떤 코드 변경을 의미할까요?"

</Reveal>
