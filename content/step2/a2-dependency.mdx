---
title: 'A-2. 의존 방향 — View가 Controller를 import하면?'
---

import { Reveal } from '../../components/Reveal'

# A-2. 의존의 방향은 단방향 — View가 Controller를 import하면?

## 선배 크루의 고민

> "1단계 domain을 그대로 두고 view만 교체한다는 방향으로 진행했는데, View에서 필요한 데이터를 가져오려다 보니 Controller를 import하게 되었습니다." — 쿠마, [PR #251](https://github.com/woowacourse/javascript-lotto/pull/251)

> "컴포넌트가 사실상 컨트롤러 역할을 하고 있는 것 같아요. 이벤트 처리, 상태 관리, 렌더링이 한 곳에 모여 있어서 분리가 어렵습니다." — 마루, [PR #300](https://github.com/woowacourse/javascript-lotto/pull/300)

> "웹 환경에서 이벤트 리스너 간 데이터를 전달하려면 어떻게 해야 할지 고민입니다. 결국 전역 상태나 DOM을 통해 공유하게 되는데 이게 맞는 건지 모르겠습니다." — 다이앤, [PR #384](https://github.com/woowacourse/javascript-lotto/pull/384)

## 리뷰어의 피드백

> "View 파일에서 Controller 파일을 import 하면 의존의 방향이 잘못된 것이라고 볼 수 있습니다." — PR #56 ([링크](https://github.com/woowacourse/javascript-lotto/pull/56))

> "MVC 패턴에서 View와 Model은 Controller를 모릅니다. 따라서 View가 Controller의 기능에 의존하면 안되고 Controller로 부터 주입을 받아야 합니다. getTotalPrize를 직접 호출 하지 말고 필요한 값은 파라미터로 받아서 그리기만 해야 합니다." — PR #56 ([링크](https://github.com/woowacourse/javascript-lotto/pull/56))

> "전반적으로 class 선택자로 요소를 찾아주시고 계시네요! class는 일반적으로 하나의 페이지 내 여러 요소에 중복적으로 들어갈 수 있고, 실제로 그렇게 사용되는게 지향되기도 해요. 그런 관점에서 봤을 때 class를 사용해서 비즈니스 로직에서 특정 요소를 찾는게 올바른지 한번 고민해보셔도 좋을 것 같습니다~!" — PR #305 ([링크](https://github.com/woowacourse/javascript-lotto/pull/305))

## AS-IS 코드

```js
// PR #56 (신세한탄) — View에서 Controller를 import하여 직접 호출하는 역방향 의존
import { getTotalPrize } from '../Controller/submitController.js';

const receiptView = {
  renderReceipt() {
    // View가 Controller의 함수를 직접 호출하여 데이터를 가져옴
    const totalPrize = getTotalPrize();
    // ... 렌더링 로직
  }
};
```

---

<Reveal>

## 탐색 미션

### PR #56 — 신세한탄 (2021, 2단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/56)

**찾아볼 것:**
1. "receiptView에서 Controller를 import한 것에 대해 리뷰어가 뭐라고 했나요? '의존의 방향이 잘못된 것'이란 구체적으로 어떤 의미인가요?"
2. "'필요한 값은 파라미터로 받아서 그리기만 해야 한다'는 것이 코드에서 어떤 모습일까요?"

### PR #305 — 소하 (2024, 2단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/305)

**찾아볼 것:**
1. "리뷰어가 EventController에서 CSS style.display를 직접 조작하는 코드에 대해 어떤 질문을 던졌나요?"
2. "비즈니스 로직에서 CSS 속성을 직접 제어하면 어떤 문제가 생길 수 있을까요?"

### PR #355 — 앵버 (2025, 1단계)
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/355)

**찾아볼 것:**
1. "리뷰어(크리스)가 '도메인 분리는 적절하다'고 하면서도 지적한 문제는 무엇인가요?"
2. "'입력/출력/데이터 생성/데이터 변경을 각각 개별적으로 추상화해야 한다'는 것이 구체적으로 어떤 코드 변경을 의미하나요?"

</Reveal>
