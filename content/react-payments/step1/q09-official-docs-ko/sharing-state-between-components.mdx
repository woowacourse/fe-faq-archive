---
title: 'Sharing State Between Components (전체 번역)'
---

# Sharing State Between Components — 전체 번역

> 원문: [react.dev/learn/sharing-state-between-components](https://react.dev/learn/sharing-state-between-components)
>
> 카드: [Q1. state는 어디서 관리하나](/react-payments/step1/q01-state-location)
>
> 정확한 표현이 필요하면 항상 원문을 함께 보세요. 번역은 자연스럽게 풀어 옮긴 의역입니다.

## 들어가며

가끔, 두 컴포넌트의 state가 *항상 함께* 바뀌어야 할 때가 있습니다. 그럴 때는 두 컴포넌트에서 state를 모두 제거하고, 가장 가까운 *공통 부모 컴포넌트*로 옮긴 다음, props로 다시 두 컴포넌트에 내려주세요. 이를 **상태 끌어올리기(lifting state up)** 라고 부르며, React 코드를 짜면서 가장 자주 하게 될 일 중 하나입니다.

## 이 문서에서 배우게 될 것

- 컴포넌트들 사이에서 state를 끌어올려 공유하는 방법
- 제어 컴포넌트(controlled)와 비제어 컴포넌트(uncontrolled)는 무엇인가

---

> ⚠️ **이 페이지는 전체 번역 진행 중**
>
> 핵심 도입부만 옮겨두었습니다. *Lifting state up by example*, *A single source of truth for each state*, *Recap*, *Try out some challenges* 등 나머지 섹션 전체 번역이 필요하면 [원문](https://react.dev/learn/sharing-state-between-components)에서 페이지 raw markdown을 *copy page*해서 코치에게 보내주세요. 같은 마크업 변환 규칙(`<Sandpack>` → 코드 블록, `<Pitfall>` → blockquote, `<DeepDive>`/`<Solution>` → `<details>`)으로 즉시 채웁니다.

## 핵심 발췌 (요약 번역)

### 예제로 보는 끌어올리기

세 단계로 진행됩니다:

1. **자식 컴포넌트에서 state를 제거** — props로 받도록 바꿉니다.
2. **하드코딩된 데이터를 부모로 이동** — 부모가 두 자식에게 같은 데이터의 *다른 측면*을 내려주도록.
3. **부모에 state 추가** — 두 자식의 *유일한 source of truth*가 부모가 되도록.

### 각 state에는 하나의 source of truth

React 앱에서, 많은 컴포넌트는 자신의 state를 가집니다. 어떤 state는 입력(input) 같은 *잎 컴포넌트(leaf)*에 가까이 있을 수 있습니다. 다른 state는 앱의 *위쪽*에 있을 수 있습니다 — 심지어 라우팅 라이브러리에 있을 수도 있습니다.

**각 state 조각마다 어느 컴포넌트가 *소유*하는지 정해야 합니다.** 이 원칙은 *single source of truth*라고도 알려져 있습니다. 모든 state가 한 곳에만 있다는 뜻이 아니라 — *각 state*가 그것을 보유하는 *특정 컴포넌트*를 가진다는 뜻입니다.

> 페이먼츠 1단계의 *프리뷰 카드*가 카드번호·만료일·CVC·브랜드를 *동시에* 보여주는 자리에서, 입력 컴포넌트와 프리뷰의 *공통 부모*가 그 state의 *소유자*가 됩니다.

### 요약

- 두 컴포넌트가 정합성을 유지해야 한다면, 그 state를 *공통 부모*로 옮기세요.
- 그다음 *공통 부모*에서 *props*로 내려보내세요.
- 마지막으로, 이벤트 핸들러를 내려보내 자식이 부모의 state를 바꿀 수 있게 하세요.
- 컴포넌트를 "제어"(부모가 props로 driven) 또는 "비제어"(자체 state로 driven)로 *생각*해보는 것이 유용합니다.

---

> 🇬🇧 *원문 전체*를 따라가려면 [Sharing State Between Components — react.dev](https://react.dev/learn/sharing-state-between-components)로 이동하세요.
>
> 🛠 *전체 번역*을 원하면 코치에게 raw markdown을 보내주세요 — 다른 페이지(예: [Choosing the State Structure 전체 번역](/react-payments/step1/q09-official-docs-ko/choosing-state-structure))처럼 우리 사이트 마크업으로 옮깁니다.
