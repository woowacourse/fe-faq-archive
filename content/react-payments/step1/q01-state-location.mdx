---
title: 'Q1. state는 어디서 관리하나'
---

import { Reveal } from '../../../components/Reveal'

# Q1. state(제어)를 어느 컴포넌트에서 관리해야 할까?

## 크루의 질문

> "state 관리(제어)를 어느 컴포넌트에서 해야 할지 오래 고민했습니다. 컴포넌트 쪼개는 기준에 대해 얘기를 많이 했습니다." — 파라디·피트·이현 페어

> "각 입력 필드를 합성하는 최상위 컴포넌트에서 가지고 있을지, custom hook 안의 useState로 가지고 반환값을 사용해서 컴포넌트를 구성할지 고민중입니다." — 콘티·먼지 페어

> "3개 이상의 컴포넌트를 통과하는 상태 props는 전역 상태로 관리하는 것이 좋은가에 대해 논의해보았습니다." — 지오·코브 페어

## AS-IS 코드

```jsx
// TBD — 크루 PR에서 발췌 예정
// 카드번호·유효기간·CVC를 각 자식 컴포넌트가 자체 useState로 보유하는 구조와,
// 부모(payment) 컴포넌트가 모든 입력 상태를 모아서 자식에 props로 내려주는 구조가
// 같은 미션 안에서 어떻게 다르게 굴러가는지 두 패턴을 나란히 두고 본다.
```

## 공식문서 단서 — *State 끌어올리기*

React 공식문서 [Sharing State Between Components](https://react.dev/learn/sharing-state-between-components) 첫 단락:

> *Sometimes, you want the state of two components to always change together. To do it, remove state from both of them, move it to their closest common parent, and then pass it down to them via props. This is known as **lifting state up**, and it's one of the most common things you will do writing React code.*

핵심 한 줄: *상태를 자식이 공유해야 할 때, 가장 가까운 공통 부모로 옮긴다.* 페이먼츠에서는 *프리뷰 카드*가 카드번호·만료일·CVC·브랜드를 모두 *동시에* 보여주므로, 입력 컴포넌트들과 프리뷰의 *공통 부모*가 어디인지가 결정의 기준이 된다.

또한 [Choosing the State Structure](https://react.dev/learn/choosing-the-state-structure)의 다섯 원칙 중 첫 번째 — *"Group related state. If you always update two or more state variables at the same time, consider merging them into a single state variable."* — 가 Q2와 직결된다.

---

<Reveal>

## 탐색 미션

### 선배 PR — *state를 자식이 보유하는 패턴*
[PR 링크 TBD](https://github.com/woowacourse/react-payments/pull/TBD)

**찾아볼 것:** "리뷰어가 state 위치에 대해 어떤 코멘트를 남겼나요? 자식 컴포넌트에 상태를 두면 *어떤 시점*에 한계가 드러나는가에 대해 어떤 단어로 설명했나요?"

### 선배 PR — *state를 부모로 끌어올린 패턴*
[PR 링크 TBD](https://github.com/woowacourse/react-payments/pull/TBD)

**찾아볼 것:** "리뷰어가 끌어올린 결정에 대해 *비용*을 어떻게 평가했나요? props drilling이 *어디서부터* 비용으로 보이는가에 대한 기준을 무엇으로 들었나요?"

### 선배 PR — *custom hook으로 옮긴 패턴*
[PR 링크 TBD](https://github.com/woowacourse/react-payments/pull/TBD)

**찾아볼 것:** "리뷰어가 hook 추출 *시점*에 대해 어떤 코멘트를 남겼나요? *처음부터 hook을 짜는 것*과 *중복을 본 뒤 추출하는 것*의 차이가 PR 대화에 어떻게 드러나나요?"

</Reveal>

## 연관 PR 더 보기

> 코덱스 후속 채움 예정 — Q01 주제로 매핑된 다른 PR 목록이 여기 들어갑니다. 195개 전체 인덱스는 [부록](/react-payments/step1/appendix-all-prs)에서 확인할 수 있습니다.
