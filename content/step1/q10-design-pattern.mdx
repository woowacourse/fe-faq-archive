---
title: 'Q10. 디자인 패턴 적용'
---

import { Reveal } from '../../components/Reveal'

# Q10. MVC 등 디자인 패턴을 어떻게 선택하고 적용해야 하나요?

## 선배 크루의 질문

> "MVC 패턴에 대한 이해없이 맹목적으로 사용했기 때문에 이번 미션 때에는 필요하지 않다면 사용하지 말아야겠다 생각했지만, 알고있는 다른 디자인 패턴이 없고 초기에 패턴을 잡지 않고 시작하려니 그것 역시 막막하여 결국 MVC 패턴을 사용하게 되었습니다." — PR #20 ([링크](https://github.com/woowacourse/javascript-lotto/pull/20))

> "최근 미션을 진행하면서 MVC에 너무 고립된 것 같아 이번에는 기초부터 돌아가자라고 생각하여 단순히 UI와 도메인 분리에 집중을 하였는데요" — PR #92 ([링크](https://github.com/woowacourse/javascript-lotto/pull/92))

> "처음부터 불필요하게 구조나 디자인 패턴을 적용하지 말자. 앱의 규모에 따라서 적절한 구조를 적용해보자" — PR #17 ([링크](https://github.com/woowacourse/javascript-lotto/pull/17))

## AS-IS 코드

```js
class LottoController {
  constructor() {
    this.model = new LottoModel();
    this.view = new LottoView();
  }

  handlePurchase(money) {
    const input = document.querySelector("#money-input");
    this.model.setMoney(Number(input.value));
    this.model.generateLottos();
    this.view.render(this.model.getLottos());
  }
}

```

---

<Reveal>

## 탐색 미션

### PR #20 — 신세한탄
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/20)

**찾아볼 것:** "리뷰어가 디자인 패턴을 '찾아보는 것'과 '경험하는 것'에 대해 어떤 관점을 제시했나요? 실제로 추천한 학습 방법은 무엇이었나요?"

### PR #92 — 동키콩
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/92)

**찾아볼 것:** "UI와 도메인만 분리한 접근에 대해 리뷰어가 어떻게 평가했나요? 전체 MVC를 적용하지 않아도 된다는 근거가 있었나요?"

### PR #17 — 체프
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/17)

**찾아볼 것:** "리뷰어가 폴더/파일 명명에 대해 어떤 피드백을 남겼나요? 명명이 설계를 어떻게 드러내는지 확인해보세요."

</Reveal>
