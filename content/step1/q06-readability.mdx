---
title: 'Q6. 가독성과 리팩터링'
---

import { Reveal } from '../../components/Reveal'

# Q6. 가독성 좋은 코드란 무엇이고, 리팩터링은 어떻게 진행해야 하나요?

## 선배 크루의 질문

> "전체적으로 코드의 가독성이 어떤지 궁금합니다!" — PR #90 ([링크](https://github.com/woowacourse/javascript-lotto/pull/90))

> "메서드내의 임시변수 제거 vs 가독성 있는 변수 생성에 대해 고민했습니다" — PR #87 ([링크](https://github.com/woowacourse/javascript-lotto/pull/87))

> "함수명이 길어지는 것에 대해 어떻게 생각하시나요? 저는 길어도 최대한 정확하게 함수 동작을 설명하는 것이 좋다고 생각합니다" — PR #10 ([링크](https://github.com/woowacourse/javascript-lotto/pull/10))

## AS-IS 코드

```js
submitPurchaseLotto(event) {
  if (event.detail < 1000) {
    alert('금액은 1000원 이상을 입력해주세요.');
    return;
  }
  const count = Math.floor(event.detail / 1000);
  this.lottos = Array.from({ length: count }, () => {
    const numbers = [];
    while (numbers.length < 6) {
      const num = Math.floor(Math.random() * 45) + 1;
      if (!numbers.includes(num)) numbers.push(num);
    }
    return numbers.sort((a, b) => a - b);
  });
  this.renderLottoList();
  this.showToggleButton();
}
```

---

<Reveal>

## 탐색 미션

### PR #87 — 결
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/87)

**찾아볼 것:** "임시변수를 쓸지 말지에 대해 리뷰어가 어떤 기준을 제시했나요? `event.detail`을 `purchaseMoney`로 바꾸는 것이 왜 가독성에 도움이 될까요?"

### PR #102 — 밧드
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/102)

**찾아볼 것:** "리뷰어가 가독성을 떨어뜨리는 원인으로 지목한 것은 무엇이었나요? AS-IS 코드에서 같은 패턴이 보이나요?"

### PR #90 — 앨버
[PR 링크](https://github.com/woowacourse/javascript-lotto/pull/90)

**찾아볼 것:** "리뷰어가 이 PR에서 가독성과 관련해 어떤 피드백을 남겼나요? 긍정적 피드백과 개선 제안이 각각 무엇이었나요?"

</Reveal>
